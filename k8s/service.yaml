apiVersion: v1
kind: Service
metadata:
  name: notes-service
spec:
  type: NodePort
  selector:
    app: notes-api   #send traffic from this service to pods having app: notes-api
  ports:
   - port: 3000  #service port
     targetPort: 3000   #pod target port 